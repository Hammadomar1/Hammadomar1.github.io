# Assets

Add these files before publishing:

- `headshot.jpg`: your professional profile photo.
- `hammad-omar-cv.pdf`: your current academic CV.

The homepage uses a remote workspace image as a temporary fallback until
`headshot.jpg` exists.
